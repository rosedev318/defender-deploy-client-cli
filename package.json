{
  "name": "defender-cli",
  "version": "0.0.1-alpha.0",
  "description": "CLI for OpenZeppelin Defender SDK",
  "main": "index.js",
  "author": "Eric Lau <ericglau@outlook.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist *.tsbuildinfo",
    "compile": "tsc -b",
    "prepare": "yarn clean && yarn compile",
    "test": "tsc -b && ava",
    "cli": "node ./dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^16.11.7",
    "@types/minimist": "^1.2.5",
    "ava": "^6.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
